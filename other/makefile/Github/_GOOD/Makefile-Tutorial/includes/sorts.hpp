#include <vector>
#include <algorithm>

typedef std::vector<int>::iterator intiter;

void bubbleSort(intiter, intiter);
void insertionSort(intiter, intiter);
void quickSort(intiter, intiter);
void mergeSort(intiter, intiter);
